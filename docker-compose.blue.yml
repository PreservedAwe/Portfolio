services:
  my-portfolio_blue:
    build: .
    container_name: my-portfolio_blue
    user: tempUser   
    networks:
      - shared-network 

networks:
  shared-network:
    external: true               